#!/usr/bin/env bash
sudo apt-get update # actualiza el repositorio de la maquina creada.
sudo apt-get -y install postgresql postgresql-contrib #instala respondiendo yes a todos la preguntas de la instalacion

